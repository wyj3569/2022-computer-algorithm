#include <bits/stdc++.h>
using namespace std;
int N;
string input, target;

void input_data() {
    cin >> input;
    cin >> target;
}

int main() {
    cin >> N;
    while (N--) {
        input_data();
    }
    return 0;
}